git config --global user.name "Satyam Kumar"
git config --global user.email "satkr06@gmail.com"
touch .gitignore    // we add those files or folders which we don't want to add in github project
git config --global init.defaultBranch master   // to make the default brach as master
git init
git add .      // files come in staging area
git commit -m "Initial Commit"    // to create commit for pushing further on github
git status  // Just to see if your working tree is clean or not
ssh-keygen -t ed25519 -C "satkr06@gmail.com"     // we run this command to generate a ssh key for our project
tail <path to id_ed25519.pub>    // to get the ssh key (which will be genetared by above command), run this command and ADD that ssh key to github account
git remote add origin <ssh url.git>    // you will get the ssh url from the repository which you will create to upload your project on github
git push origin master

